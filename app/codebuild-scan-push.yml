version: 0.2
phases:
  pre_build:
    commands:
      - $(aws ecr get-login)
  build:
    commands:
      - docker build --tag "${DOCKER_REG}/demo-app:${CODEBUILD_BUILD_NUMBER}" .
  post_build:
    commands:
      - docker push "${DOCKER_REG}/demo-app:${CODEBUILD_BUILD_NUMBER}"

